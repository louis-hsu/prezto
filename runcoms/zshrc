# ------------------------ General/System
setopt cdablevars                           # Set default zsh options (setopt)

# Source Prezto.
if [[ -s "${ZDOTDIR:-$HOME}/zprezto/init.zsh" ]]; then
    source "${ZDOTDIR:-$HOME}/zprezto/init.zsh"
fi

source virtualenvwrapper.sh                 # Configure python virtualenvwrapper
pythonPath=$(python3 -c "import site; print(site.getsitepackages()[0])")  # Export 'site-package' path
export PYTHONPATH="${pythonPath}"

eval "$(zoxide init --cmd cd zsh)"          # Configure 'zoxide' to replace 'cd'

# Add 'brew' path in $FPATH before running autoload for autocompletion
if type brew &>/dev/null
then
  FPATH="${FPATH}:$(brew --prefix)/share/zsh/site-functions"

fi
fpath+=$XDG_CONFIG_HOME/zsh/zfunc           # Also add 'zfunc' in $FPATH


# ------------------------ Alias/Keybindings/Functions
# Move alias definitions to separted file (.zsh_aliases)
if [ -f ${ZDOTDIR:-$HOME}/.zsh_aliases ]; then
    . ${ZDOTDIR:-$HOME}/.zsh_aliases
fi

source <(fzf --zsh)													# 'fzf' integration
# source ~/.config/zsh/fzf-bindings.zsh				# Remove 'fzf' keybindings and apply mine

# # Remove original and re-bind hotkeys for fzf widgets
# bindkey -r vicmd '\ec'
# bindkey -r viins '\ec'
# bindkey -r vicmd '^T'
# bindkey -r viins '^T'
# bindkey -M viins '^f' fzf-file-widget       # 'Ctrl-f' to list files
# bindkey -M vicmd '^f' fzf-file-widget
# bindkey -M viins '^t' fzf-cd-widget         # 'Ctrl-t' to move into directory
# bindkey -M vicmd '^t' fzf-cd-widget

# function open_with_vim_widget() {
#   local _files
#
#   zle -I
#
#   _files=$(~/.bin/fzf-nvim | sed 's/\x1b\[[0-9;]*m//g' | sed 's/[[:space:]]*$//')
#
#   if [[ -n $_files ]]; then
#     echo -n "$_files" | xargs nvim
#   fi
#
#   zle reset-prompt
# }
# zle -N open_with_vim_widget
# bindkey -M viins '^e' open_with_vim_widget
# bindkey -M vicmd '^e' open_with_vim_widget
#
# # Create 'ctrl-e' to open file in 'nvim'
# function open_with_vim_widget() {
#   local _files
#
#   zle -I
#
#   _files=$(
#     fd --type f --color=always --hidden 2>/dev/null \
#     | if [[ -n $TMUX ]]; then
#         fzf-tmux ${=FZF_TMUX_OPTS} --ansi \
#           --preview 'bat --color=always --line-range :200 {} 2>/dev/null || cat {}'
#       else
#         fzf ${=FZF_DEFAULT_OPTS} --ansi \
#           --preview 'bat --color=always --line-range :200 {} 2>/dev/null || cat {}'
#       fi \
#     | sed 's/\x1b\[[0-9;]*m//g' \
#     | sed 's/[[:space:]]*$//'
#   )
#
#   if [[ -n $_files ]]; then
#     echo -n "$_files" | xargs nvim
#   fi
#
#   zle reset-prompt
# }
# zle -N open_with_vim_widget
# bindkey -M viins '^e' open_with_vim_widget
# bindkey -M vicmd '^e' open_with_vim_widget
#
# # Create 'ctrl-e' to open file in 'nvim'
# function open_with_vim_widget() {
#   local _temp _files
#
#   zle -I  # tell ZLE we're about to do terminal I/O
#   _temp="$(__fzf_select)"
#   _files="${_temp%[[:space:]]}"  # strip trailing whitespace (pure zsh, no sed)
#
#   if [[ -n $_files ]]; then
#     echo -n "$_files" | xargs nvim
#   fi
#
#   zle reset-prompt  # always redraw prompt, even on <Esc> / empty selection
# }
# zle -N open_with_vim_widget
# bindkey -M viins '^e' open_with_vim_widget
# bindkey -M vicmd '^e' open_with_vim_widget

# ------------------------ Others
eval "$(register-python-argcomplete pipx)"  # Autocompletion for 'pipx'

# 'fzf-tab' integration and configurations

# Solve 'fzf-tab' doesn't show dot-file/directory issue
# and ignore specific files or folders
# https://github.com/Aloxaf/fzf-tab/issues/476
#_comp_options+=(globdots)
setopt glob_dots
zstyle ':completion:*' ignored-patterns '.DS_Store' '.git' "Icon$'\r'"

# Hightlight dnagerous 'rm -f' command -- Louis 2026/0120
# 1. Enable PCRE (Perl Compatible Regular Expressions)
# This is required for \b (word boundaries) and complex groups to work
zmodload zsh/pcre
setopt RE_MATCH_PCRE

# 2. Define the Highlighter Rules
typeset -A ZSH_HIGHLIGHT_REGEXP

# 3. Add the Rule
# Logic: "rm" -> space -> optional other flags -> flag containing "f"
# Style: White text on Red background (danger!)
# Add '[^|;&]*' at the end to match anything EXCEPT separators
ZSH_HIGHLIGHT_REGEXP+=('\brm\s+(?:-[a-zA-Z]+\s+)*-[a-zA-Z]*f[^|;&]*' 'fg=white,bold,bg=red')

# ------------------------ Hacks/Hooks
# Prompt indicator of subshell triggered by 'yazi'
YAZI_TERM='
[%(!.%B%F{9}%n@%m%f%b.%F{yellow}%n@%m%f):%F{4}${_prompt_nshiu_pwd}%f]${git_info:+${(e)git_info[status]}${(e)git_info[indicator]}}
${python_info:+${(e)python_info[virtualenv]}}%(!. %B%F{9}# %f%b.)%F{12}[ yazi]%f ${editor_info[keymap]} '
if [ -n "$YAZI_LEVEL" ]; then
  PS1="$YAZI_TERM"
fi

# Add 'onefetch' to zsh hook:
# 1. Define 'onefetch' logic
function check_directory_for_new_repository() {
  current_repository=$(git rev-parse --show-toplevel 2> /dev/null)

  if [ "$current_repository" ] && \
     [ "$current_repository" != "$last_repository" ]; then
    # -d: Disable fields that take up space
    # --no-color-palette: Keep it sleek
    # --nerd-fonts: Use icons instead of text blocks
    onefetch -d license churn url head --no-color-palette --nerd-fonts
  fi
  last_repository=$current_repository
}
# 2. Use zsh 'chpwd' hook to trigger 'check_directory_for_new_repository'
autoload -U add-zsh-hook
add-zsh-hook chpwd check_directory_for_new_repository
# 3. Initial check while a shell is startup
check_directory_for_new_repository

# ------------------------ Must be run in the end
# Run compinit to enable autocmopletion defined in /opt/homebrew/share/zsh/site-functions
autoload -Uz compinit
compinit

# ------------------------ TODO
# # Hook 'direnv' to zsh -- Louis 2024/1103
# # Remove 'direnv' due to possible conflict with 'mice' -- Louis 2026/0126
# eval "$(direnv hook zsh)"

# TODO
# 1. If Ruby/Gem rbenv is required and how to install/configure
# Ruby/gem configuration -- Louis 2015/0203
# Since OS X 'locks' gem packages in /Library/Ruby/Gems with root privilege, I cannot install gem packages
# with my own account unless run 'sudo', which is a bad idea
# Install rbenv instead to create private Ruby environment to fix this issue
#if which rbenv > /dev/null; then eval "$(rbenv init -)"; fi

# TODO
#  1. If multiple Java environment is required
#  2. If yes, if jenv is the one for multi-java management
# Install jenv for Java version control -- Louis 2016/0905
# Remove jenv setup since I might not really need multiple Java version
# export PATH="$HOME/.jenv/bin:$PATH"
# export JENV_ROOT=/usr/local/opt/jenv
# if which jenv > /dev/null; then eval "$(jenv init -)"; fi

# ------------------------ Obsolete
# bindkey -v                                    # done in prezto already
# shopt -s histappend                           # append to the history file, don't overwrite it
# ZSH_AUTOSUGGEST_HIGHLIGHT_STYLE="fg=#878787"  # done in prezto already

# Customize to your needs...
#case "$TERM" in
#xterm|rxvt)
#    export LSCOLORS=deadfxdxcxegedabagacad
#    ;;
#*)
#    if [[ -s "${ZDOTDIR:-$HOME}/.zprezto/init.zsh" ]]; then
#        source "${ZDOTDIR:-$HOME}/.zprezto/init.zsh"
#    fi
#    export LSCOLORS=exfxcxdxbxegedabagacad
#    ;;
#esac

# Perl 5 library location -- Louis 2022/1011
# Uninstall brew version 'perl' and comment out related setup since cannot
# resolve lib location customization.
# -- Louis 2024/1028
#eval "$(perl -I$HOME/.perl5lib/lib/perl5 -Mlocal::lib=$HOME/.perl5lib)"

# # ----------------------- Hack of command not found
# # Parse prefix 'tp' to enable tmux popup feature
# # https://unix.stackexchange.com/questions/697465/how-do-i-create-a-command-prefix-for-bash-with-an-alias
# # -- Louis 2022/0916
# #
# # 1. Change prefix from 'tp' to 'tf' to align 'tmuxfloat' initial
# # 2. Re-write 'tmuxfloat' to fix unexpected quitting issue
# # 3. However, need to think if keeping this hack or simpily using 'tf' or Ctrl+P
# #    to trigger floating display
# # -- Louis 2024/0312
#
# command_not_found_handler() {
#   # read the "missing command" entered
#   missingcmd="$1"
#   # remove it from the argument list
#   shift
#   # check if starts with "tf"
#   if [[ "${missingcmd:0:2}" == "tf" ]] ; then
#     # read part after "tf"
#     prefixedcmd="${missingcmd:2}"
#     # check if it's a real command
#     #echo "$(which ${prefixedcmd})"
#     if which "$prefixedcmd" &> /dev/null ; then
#       #tmux popup -d "#{pane_current_path}" -h 80% -w 80% -e LESS='-i -M -R -W -X -z-4 -g -j4' -e BAT_PAGER='less -R' -E "$prefixedcmd" "$@"
#       tmuxfloat $prefixedcmd $@
#       return $?
#     fi
#   fi
#   # default command not found behavior
#   echo "zsh: command not found: $missingcmd" >&2
#   return 127
# }
